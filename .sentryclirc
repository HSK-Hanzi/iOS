[defaults]
# Read by sentry-cli (e.g. the release/dSYM upload build phase). The auth token is supplied out of
# band via SENTRY_AUTH_TOKEN, never committed here.
org = zili
project = 4511725834928128
