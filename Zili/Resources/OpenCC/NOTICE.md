# OpenCC conversion data

`STConversion.txt` is a simplified→traditional conversion table derived from the
[Open Chinese Convert (OpenCC)](https://github.com/BYVoid/OpenCC) project's `STPhrases.txt`
and `STCharacters.txt` dictionaries, merged into a single tab-separated `key<TAB>traditional`
file keeping only each entry's first (default) traditional form. It targets OpenCC's standard
traditional forms (mainland norm), not a regional Taiwan/Hong Kong variant.

Licensed under the Apache License 2.0, the same terms as the upstream OpenCC dictionaries.
